# Sample Data

The real smartphone IMU recordings are not included in this public portfolio repository.

Use `python src/har_pipeline.py --synthetic` to generate an in-memory synthetic dataset and run the public-safe example pipeline.

